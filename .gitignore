# -----------------------------------------------------------------------------
# Binaries and Built Files
# -----------------------------------------------------------------------------
# Binaries for programs and plugins
*.exe
*.exe-
*.dll
*.so
*.dylib

# Test binaries generated by `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Common build outputs / custom binary directories
/bin/
/dist/
/target/

# -----------------------------------------------------------------------------
# Dependency Directories
# -----------------------------------------------------------------------------
# Uncomment if you manage dependencies via `vendor/` and prefer not to commit them
# /vendor/

# -----------------------------------------------------------------------------
# Environment & IDEs
# -----------------------------------------------------------------------------
# Local environment variables
.env
*.env

# JetBrains / GoLand
.idea/
*.iws
*.iml
*.ipr

# Visual Studio Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# macOS / OS artifacts
.DS_Store
Thumbs.db

.echosight-server
echosight-server